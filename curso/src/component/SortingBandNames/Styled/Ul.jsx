import styled from 'styled-components';

const Ul = styled.ul`
    margin: 0;
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: space-around;
`;

export default Ul;